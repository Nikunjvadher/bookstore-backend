services:
          - type: web
            name: bookstore-backend
            env: node
            region: oregon
            buildCommand: npm run build
            startCommand: npm start
            envVars:
                      - key: NODE_ENV
                        value: production
